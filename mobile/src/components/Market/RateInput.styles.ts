import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    input: {
        minWidth: 84,
        textAlign: 'right',
        borderRadius: 6,
        paddingHorizontal: 8,
        paddingVertical: 6,
    },
});